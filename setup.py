from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='these is the testing of the gradion and cookies cutter',
    author='Prince',
    license='',
)
